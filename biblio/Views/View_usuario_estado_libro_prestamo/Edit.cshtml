@model biblio.Models.View_usuario_estado_libro_prestamo

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>View_usuario_estado_libro_prestamo</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.nomusuario)

        @Html.HiddenFor(model => model.telusuario)

        @Html.HiddenFor(model => model.estadousuario)

        @Html.HiddenFor(model => model.titulolibro)

        @Html.HiddenFor(model => model.ndias)

        @Html.HiddenFor(model => model.fechaprestamo)

        @Html.HiddenFor(model => model.fechadevo)

        @Html.HiddenFor(model => model.observacion)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
